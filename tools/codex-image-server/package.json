{
  "name": "codex-image-server",
  "version": "1.0.0",
  "private": true,
  "description": "Ponte local: app -> geracao de imagem via conta ChatGPT (Codex)",
  "bin": { "codex-image-server": "server.js" },
  "scripts": { "start": "node server.js", "mock": "MOCK=1 node server.js" }
}
